#!/bin/bash
[ -f .env ] && source .env

# export S3_INPUT_PATH="s3://nesta-ds-projects/<MISSION OR PRACTICE>/<PROJECT NAME>"

# Activate the conda environment
eval "$(conda shell.bash activate "asf_hp_cost_estimator_model")"
[ "$CONDA_DEFAULT_ENV" = "asf_hp_cost_estimator_model" ] || echo "Have you run \`make install\`?"
